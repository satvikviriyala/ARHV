# Local dev + tests (not deployed). Lambda deps live in each function's / the layer's requirements.txt.
pytest>=8
moto[dynamodb,s3,secretsmanager,cloudformation]>=5
boto3>=1.34
PyJWT>=2.8,<3
cedarpy==4.12.0
strands-agents[ollama]>=1.56,<2
ruff>=0.6
numpy>=1.26
pillow>=10
